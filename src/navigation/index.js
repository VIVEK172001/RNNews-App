import React,{useState,useEffect} from 'react';
import {
  SafeAreaView,
  StyleSheet,
  ScrollView,
  View,
  Text,
  StatusBar,
} from 'react-native';

import Routes from "./Routes";

const Providers=()=>{
    return(
        <Routes/>
    );
};

export default Providers;